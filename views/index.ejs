<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🚲</text></svg>">
    <title>Macclesfield Cycling Weather</title>
    <style>
        body {
            font-family: 'Comic Sans MS', cursive;
            color: white;
            text-align: center;
            padding: 2rem;
            transition: background-color 0.5s;
            min-height: 100vh;
            margin: 0;
            background-image: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4));
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: rgba(0, 0, 0, 0.6);
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        .info-box {
            margin: 1rem 0;
            padding: 1rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.05);
            transition: transform 0.3s, box-shadow 0.3s;
            backdrop-filter: blur(5px);
        }
        .info-box:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }
        .wonkiness-meter {
            width: 200px;
            height: 200px;
            margin: 1rem auto;
            font-size: 5rem;
            transition: transform 0.3s ease-in-out;
        }
        h1 {
            color: #FFD700;
            text-shadow: 0 0 10px rgba(255, 215, 0, 0.5);
            font-size: 2.5rem;
            margin-bottom: 2rem;
        }
        h2 {
            color: #FFD700;
            font-size: 1.5rem;
            margin-bottom: 1rem;
            text-shadow: 0 0 5px rgba(255, 215, 0, 0.3);
        }
        p {
            font-size: 1.2rem;
            line-height: 1.6;
        }
        .info-box p {
            opacity: 0.9;
        }
        .info-box:hover p {
            opacity: 1;
        }
        .wind-speed {
            font-size: 1rem;
            opacity: 0.8;
            margin-top: 0.5rem;
        }
    </style>
</head>
<body style="background-color: <%= backgroundColor %>">
    <div class="container">
        <h1>🚲 Macclesfield Cycling Weather 🌤️</h1>
        
        <div class="info-box">
            <h2>Will it rain on Thursday at 19:45? 🌧️</h2>
            <p><%= willItRain %></p>
        </div>

        <div class="info-box">
            <h2>Is it Dave Windy? 💨</h2>
            <p><%= isWindy %></p>
            <p class="wind-speed">Wind speed: <%= windSpeed %> mph</p>
        </div>

        <div class="info-box">
            <h2>Hanno Lateness Predictor</h2>
            <p>Expected Lateness: <%= hannoLateness %> minutes ⏰</p>
        </div>

        <div class="info-box">
            <h2>Compass's Helmet Wonkiness</h2>
            <div class="wonkiness-meter" style="transform: rotate(<%= helmetWonkiness %>deg)">
                🪖
            </div>
            <p>Wonkiness: <%= helmetWonkiness %>°</p>
        </div>

        <div class="info-box">
            <h2>Sunset Time ⛅</h2>
            <p><%= sunset %></p>
        </div>
    </div>
</body>
</html> 